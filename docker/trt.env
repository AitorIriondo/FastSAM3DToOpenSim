# TRT mode — use after building .engine files inside the container.
# docker compose run --rm --env-file docker/trt.env fastsam3d python demo_video_opensim.py ...
FOV_TRT=1
USE_TRT_BACKBONE=1
USE_COMPILE=1
DECODER_COMPILE=1
COMPILE_MODE=reduce-overhead
COMPILE_WARMUP_BATCH_SIZES=1
